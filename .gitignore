.build/
build/
.DS_Store
Resources/*.onnx
Resources/*.bin
# Les images que les harnais écrivent — RenduCheck, SpectreCLI, --rendu, --photo.
# Elles sont regardées puis jetées ; aucune n'a de raison d'être commise.
*.ppm
.vscode/
.claude/settings.local.json
